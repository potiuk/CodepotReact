'use strict';

var React = require('react-native');

var {
  StyleSheet
} = React;

var CommonStyles = StyleSheet.create({
  button: {
    margin: 10,
    padding: 10,
    paddingRight: 20,
    paddingLeft: 20,
    borderWidth: 1,
  },
});

module.exports = CommonStyles;
